<img src="<%= @activity.img_url %>" class="card-img-top main-image" alt="<%= @activity.name %> Poster">
<div class="container">
  <div class="d-flex flex-column">
    <div class="d-flex justify-content-between align-items-center mt-4">
      <h3><%= @activity.name %></h3>
      <p><%= @activity.price %><span class="h6">€</span></p>
    </div>
    <div class="d-flex justify-content-between align-items-end mb-4 mt-4">
      <h6><i data-feather="map-pin" class="location_pin"></i><%= @activity.location %></h6>
      <p class="text-end"><i data-feather="clock" class="w-25"></i> <%= @activity.duration %></p>
    </div>
    <div class="d-flex justify-content-between">
      <p class="description w-50">Hatha yoga focuses on posture and breathing techniques, traditionally to channel vital energy source. In Sanskrit, Hatha translates to force. The practice involves breath, body, and mind, and classes are usually 45 minutes to 90 minutes of breathing, yoga poses, and meditation</p>
    </div>
    <div class="d-flex justify-content-between align-items-center">
      <div class="d-flex align-items-center mt-4">
        <img src="<%= @activity.user.avatar_url %>" alt="" class="rounded-circle smaller-img">
        <p class="m-0 py-0 owner">
          <%= @activity.user.first_name%> <%= @activity.user.last_name %>
        </p>
      </div>
      <div>
        <%= link_to "Book", "#", class: "btn btn-primary"%>
      </div>
    </div>
  </div>
</div>
